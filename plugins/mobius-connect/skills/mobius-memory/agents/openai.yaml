interface:
  display_name: "Mobius session memory"
  short_description: "Inspect session ancestry and original references"
  default_prompt: "Use $mobius-memory to inspect the ancestry of the session I select."
policy:
  allow_implicit_invocation: false
